kind: pipeline
name: default

steps:
- name: test
  image: node
  commands:
  - npm install
  - npm install -g newman
  - node index.js
  - newman --version
  - newman run NS-Node-Eval.postman_collection.json